import styled from "styled-components";

const HeaderWrapper = styled.div`
  display: flex;
  flex-direction: row;
  h2 {
    margin: 0;
    font-size: 48px;
    font-weight: bold;
  }

  img {
    padding-top: 2rem;
  }
`;

export default HeaderWrapper;
