import React from "react";
import { render, fireEvent } from "@testing-library/react";
import RoomLinkSender from "./index";
import theme from "../../theme";
import { ThemeProvider } from "styled-components";

test("RoomLinkSender matches snapshot", () => {});

test("RoomLinkSender renders a given link", () => {});

test("RoomLinkSender renders when there is no link", () => {});

test("Typing in the email input fires updates the email state", () => {});

test("RoomLinkSender requires valid email when Send Invite button is clicked", () => {});

test("RoomLinkSender send invite email when Send Invite button is clicked", () => {});
