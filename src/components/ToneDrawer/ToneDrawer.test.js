import React from "react";
import { render, fireEvent } from "@testing-library/react";
import ToneDrawer from "./index";
import theme from "../../theme";
import { ThemeProvider } from "styled-components";

test("ToneDrawer matches snapshot", () => {});

test("ToneDrawer renders grid of tones", () => {});

test("ToneDrawer updates selected tone when a ToneButton is clicked", () => {});
