import React from "react";
import { render, fireEvent } from "@testing-library/react";
import RoomCreator from "./index";
import theme from "../../theme";
import { ThemeProvider } from "styled-components";

test("RoomCreator matches snapshot", () => {});

test("RoomCreator renders New Chat button", () => {});

test("RoomCreator renders RoomName input when New Chat button is clicked", () => {});

test("RoomCreator creates room when Create Room button is clicked", () => {});
